{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "docker",
            "type": "shell",
            "command": "docker compose up -d",
            "runOptions": {
                "runOn": "folderOpen"
            }
        },
        {
            "type": "npm",
            "script": "start:dev",
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}